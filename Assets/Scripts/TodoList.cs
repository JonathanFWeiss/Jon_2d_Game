using UnityEngine;

//TODO
// - Add sound effects for hops, dashes, and death
// fix ability to change direction during attack
// - Add a short stun effect when the crow is hit by the player's attack
// add player hp




public class TodoList : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
