using UnityEngine;

public class RoomEntryPoint : MonoBehaviour
{
    [Tooltip("ID used by a RoomTransition targetEntryId to place the player here after loading this scene.")]
    public string entryId;
}
